{
  "name": "reaction-tester",
  "version": "1.0.0",
  "description": "reaction tester game",
  "main": "index.js",
  "scripts": {
    "mkdir": "mkdir -p build",
    "build": "npm run clean && npm run mkdir && npm run build:html && npm run build:css",
    "watch": "npm run watch:html & npm run watch:css",
    "clean": "rm -rf build",
    "push": "npm run git:gh-pages && npm run git:add && npm run git:commit && npm run git:gh-push",
    "rebase": "npm run git:master && npm run git:rebase && npm run git:master-push && npm run git:gh-pages",
    "build:html": "npm run clean:html && cp index.html build/",
    "watch:html": "npm run build:html && chokidar index.html -c 'npm run build:html'",
    "clean:html": "rm -f build/index.html",
    "build:css": "npm run clean:css && lessc --source-map css/index.less build/$npm_package_name.$npm_package_version.css",
    "watch:css": "npm run build:css && chokidar 'css/**/*.less' -c 'npm run build:css'",
    "clean:css": "rm -f build/$npm_package_name.$npm_package_version.css build/$npm_package_name.$npm_package_version.css.map",
    "git:gh-pages": "git checkout gh-pages",
    "git:add": "git add .",
    "git:commit": "git commit -m 'LESS installed'",
    "git:gh-push": "git push origin gh-pages",
    "git:master": "git checkout master",
    "git:rebase": "git rebase gh-pages",
    "git:master-push": "git push origin master"
  },
  "author": "David Trinh",
  "license": "ISC",
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "less": "^2.6.1"
  }
}
